_schema-version: '3.2'
ID: logaligroup
version: 0.0.1
modules:
  - name: logaligroup1-approuter
    type: approuter.nodejs
    path: logaligroup1-approuter
    requires:
      - name: logaligroup1_html_repo_runtime
      - name: dest_logaligroup
    parameters:
      disk-quota: 256M
      memory: 256M
  - name: logaligroup1_ui_deployer
    type: com.sap.application.content
    path: .
    requires:
      - name: logaligroup1_html_repo_host
        parameters:
          content-target: true
    build-parameters:
      build-result: resources
      requires:
        - artifacts:
            - Employee-content.zip
          name: Employee
          target-path: resources/
  - name: Employee
    type: html5
    path: Employee
    build-parameters:
      builder: custom
      commands:
        - npm run build
      supported-platforms: []
resources:
  - name: logaligroup1_html_repo_runtime
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-plan: app-runtime
  - name: logaligroup1_html_repo_host
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-plan: app-host
  - name: dest_logaligroup
    parameters:
      service-plan: lite
      service: destination
    type: org.cloudfoundry.managed-service
build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm install
